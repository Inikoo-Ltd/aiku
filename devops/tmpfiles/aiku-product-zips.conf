# Reap leaked product image zip exports (ProductZipExport writes to /tmp,
# deleteFileAfterSend never runs when the client aborts the download).
# Install: cp to /etc/tmpfiles.d/ on boro and helio.
e /tmp/product_images_* - - - 1d
