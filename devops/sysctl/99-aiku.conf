# neon runs Postgres, staging and CI on one box. At the stock 60 the kernel
# swaps out live anonymous memory to keep cache, and a heavy pest run fills all
# 31G of swap, starving Postgres of I/O until a backend dies.
vm.swappiness = 10
